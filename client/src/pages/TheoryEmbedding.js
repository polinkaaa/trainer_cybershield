import React from "react";
import NavBar from "../components/NavBar";

const Embedding = () => {
    return (
        <div>
            <NavBar />
            Embedding
        </div>
    );
};

export default Embedding;