import React from "react";
import NavBar from "../components/NavBar";

const Detection = () => {
    return (
        <div>
            <NavBar />
            Detection
        </div>
    );
};

export default Detection;