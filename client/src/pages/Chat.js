import React from "react";
import NavBar from "../components/NavBar";

const Chat = () => {
    return (
        <div>
            <NavBar />
            CHAT
        </div>
    );
};

export default Chat;